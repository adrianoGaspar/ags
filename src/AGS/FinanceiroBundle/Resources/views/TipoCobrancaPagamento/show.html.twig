{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TipoCobrancaPagamento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tipocobrancapagamento</th>
                <td>{{ entity.tipoCobrancaPagamento }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipocobrancapagamento') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipocobrancapagamento_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
