{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Fornecedor list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Statusfornecedor</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('fornecedor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.statusFornecedor }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('fornecedor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('fornecedor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('fornecedor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
