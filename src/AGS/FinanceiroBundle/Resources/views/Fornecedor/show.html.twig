{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Fornecedor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Statusfornecedor</th>
                <td>{{ entity.statusFornecedor }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('fornecedor') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('fornecedor_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
