{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Servico list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Servico</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('servico_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.servico }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('servico_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('servico_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('servico_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
