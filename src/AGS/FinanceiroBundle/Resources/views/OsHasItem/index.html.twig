{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OsHasItem list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Qtd</th>
                <th>Valor</th>
                <th>Un</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('oshasitem_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.qtd }}</td>
                <td>{{ entity.valor }}</td>
                <td>{{ entity.un }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('oshasitem_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('oshasitem_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('oshasitem_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
