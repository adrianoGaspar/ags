{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OsHasItem</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Qtd</th>
                <td>{{ entity.qtd }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ entity.valor }}</td>
            </tr>
            <tr>
                <th>Un</th>
                <td>{{ entity.un }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('oshasitem') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('oshasitem_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
